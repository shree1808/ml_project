<!-- ## End to End ML project -- Inspired by Krish Naik

* Creating conda venv in VSC

>> cd project folder
>> code . // new VSC env
>> conda create -p venv python==3.8 -y (creating env)
>> cls
>> conda activate venv/ (activating env)


* Synching venv with the Github Repo

>> git init
>> git add README.md (add your custom readme file)
>> git commit -m "First Commit"
>> git branch -M main (Creating the Main/Root)
>> git remote add origin <url>
>> git push -u origin main


* creating the ignore file


 -->
